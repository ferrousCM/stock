# PRD — 모의투자(페이퍼 트레이딩) 기능

- **상태**: 0~5단계 완료 + `schedule`(자동 cron, 평일 19:30 KST) 활성화까지 완료(2026-08-14) — 이제 평일마다 자동으로 돈다. 7단계(국내+해외+코인 확장)에 이어 **8단계(봇 다중화 — 기본형/공격적/모멘텀, 2026-08-22)까지 완료**. 다음은 6단계에서 미뤄둔 관찰(영향 관찰은 계속 진행 중)과 9장의 향후 확장 검토 — 10장 참고 — v4 (매매 의사결정을 **LLM 기반 → 규칙 기반(결정론적)으로 전환**. 유료 서비스인 Claude API 키 발급·비용을 지금 단계에서는 넘기고 싶다는 사용자 결정에 따름. LLM 경로는 폐기가 아니라 9장에 향후 옵션으로 보존)
- **v2 (신규, 2026-08-22 설계 착수, 구현 전)**: 기존 v1과 완전히 분리된 두 번째 매매 파이프라인. `tradermonty/claude-trading-skills`(Claude Code 스킬 마켓플레이스, 별도로 `~/.claude/skills/`에 설치)의 방법론(포지션사이징·기술적 분석·모멘텀 버스트 스크리닝)을 **결정론적 파이썬 코드로 포팅**해 v1과 나란히 돌린다. 상세 설계는 **11장**. v1 원장·워크플로·UI는 전혀 건드리지 않는다.
- **관련 문서**: [CLAUDE.md](./CLAUDE.md) `## 모의투자(페이퍼 트레이딩) 기능` 섹션에 컨벤션 요약
- **스코프**: 이 문서는 기존 스크리닝/차트/예측 대시보드에 새로 추가하는 "모의투자" 기능 하나에 한정한다. 기존 기능의 PRD를 소급 작성하지 않는다.

## 1. 배경 · 목표

가상의 현금 **1억원**으로 시작해서, 이미 구축된 가격 예측(`predictor.py`) · 스크리닝(`screener.py`) 기능을 활용해 KOSPI/KOSDAQ 종목을 자동으로 매매하고, 누적되는 손익을 투자금에 계속 반영하며, 그 과정과 실적을 대시보드 링크로 지인들과 공유한다.

**핵심 결정 (사용자 확정)**:
- **단일 공유 포트폴리오**. 로그인/인증 없음. 링크를 여는 모든 사람이 동일한 하나의 포트폴리오 현황을 본다.
- **매매 의사결정은 우선 규칙 기반(결정론적) 알고리즘으로 구현한다.** 원래 설계는 Claude API 기반 판단형 에이전트였으나(9장에 원안 보존), "유료 서비스라 지금은 키 발급 없이 진행하겠다"는 결정에 따라 예측치·RSI·뉴스감성에 임계값을 적용하는 규칙 엔진으로 대체한다. API 비용·키 관리가 전혀 없어지고, 이 프로젝트에 이미 있는 패키지만으로 구현된다(신규 의존성 0개).
- **매매는 유저의 대시보드 방문과 완전히 무관하게, 독립적으로 자동 실행된다.** 유저는 결과(매매 이력·투자금 변동)를 대시보드에서 **읽기만** 한다 — 방문이 실행을 유발하지 않는다. (→ v1 초안에 있던 "방문 시 지연 실행" 트리거는 이 요구사항과 맞지 않아 v2에서 폐기하고 5.5에서 재설계했다.)
- **실행 범위는 하루 1회**로 확정 (사용자 선택). 장중(09:00~17:00) 내내 주기적으로 새로 매매 판단을 반복하는 것은 검토 결과 채택하지 않았다 — 이유는 2장 하단 "검토했지만 채택하지 않은 것" 참고.

## 2. 범위

### V1 in scope
- 코스피/코스닥 + 해외증시(나스닥) + 코인(업비트) 유니버스 대상 일별 매매 신호 생성 (기존 예측 엔진 확장, 10장 7단계에서 국내 전용→3개 시장으로 확장)
- 규칙 기반(결정론적) 매매 의사결정 + 리스크 가드레일
- 포트폴리오 원장(현금·보유종목·거래내역) 영속 저장
- **GitHub Actions 스케줄러**로 매일 1회, 방문자 유무와 무관하게 자동 실행
- 성과 리포팅 신규 화면(총자산 추이, 보유종목, 거래내역, 코스피 대비 초과수익률) — 순수 읽기 전용

### V1 out of scope (향후 확장 — 9장 참고)
- 멀티유저 포트폴리오 / 로그인
- 백테스트 엔진
- 실제 증권사 연동 (애초에 전 기능이 시뮬레이션이며 실거래 없음)

### 검토했지만 채택하지 않은 것 — 장중 실시간 모니터링

처음엔 "09:00~17:00 동안 자동으로 작동해서 손익을 지속적으로 반영"하는 그림을 검토했다. 기술적으로 확인해보니:

- 이 프로젝트의 시세 데이터는 `predictor.py`/`screener.py` 모두 **일봉(하루 종가) 기준**이다. 장중에 예측 로직을 여러 번 다시 돌려도, 그날 종가가 확정되기 전까지는 새로운 정보가 생기지 않는다 — 즉 하루에 여러 번 LLM 매매 판단을 반복하는 건 같은 질문을 반복하는 것과 다르지 않다.
- 진짜 장중 실시간(현재가 기준 손절/익절, 실시간 평가금액)을 구현하려면 지금 이 프로젝트에 없는 **실시간 시세 데이터 소스**가 새로 필요하다 (증권사 Open API 계좌 개설, 또는 유료 시세 벤더) — 스코프가 이 기능 전체보다 커질 수 있는 별도 통합 작업.
- 그래서 **V1은 하루 1회 자동 실행**으로 확정했다. 장중 모니터링은 향후 확장(9장)으로 남겨둔다.

## 3. 용어 정리 — "에이전트"의 두 가지 뜻

이 프로젝트 작업 과정에서 "에이전트"가 두 문맥으로 쓰인다. 혼동 방지를 위해 명시한다.

| | 이 문서에서 다루는 것 |
|---|---|
| **Claude Code 서브에이전트** (메인/코딩/검증 에이전트) | 이 기능을 **개발하는** 도구. `클로드 에이전트/` 폴더에 별도 정리됨. 이 PRD의 대상이 아니다. |
| **가격예측 에이전트 / 투자전략 에이전트** | 매일 자동 실행되는 파이썬 스크립트 **안에서 도는** 로직. 이 PRD가 설계하는 대상. |

## 4. 시스템 구성 — 실행 환경과 조회 환경의 분리

**이 기능의 핵심 아키텍처 원칙: "매매를 실행하는 곳"과 "결과를 보여주는 곳"을 완전히 분리한다.** 유저는 방문 시 결과만 읽으므로, 매매 로직이 살아있는 Streamlit 앱 프로세스 안에 있을 이유가 없다 — 오히려 없는 게 더 단순하고 안전하다(Streamlit 다중 방문자가 우연히 겹쳐 중복 실행될 위험 자체가 사라진다).

```
┌─────────────────────────────── GitHub Actions (실행 환경) ───────────────────────────────┐
│  매일 1회, 평일 19:30 KST 전후(장 마감 이후, 실측 근거 5.5) — 방문자 유무와 무관하게 트리거 │
│                                                                                              │
│   screener.py(기존) → 워치리스트                                                            │
│         │                                                                                    │
│         ▼                                                                                    │
│   ① 가격예측 에이전트 (predictor.py 확장)  → 후보별 예측 신호                                │
│         │                                                                                    │
│         ▼                                                                                    │
│   ② 매매 규칙 엔진 (결정론적 코드, 신규)   → 임계값 기반 매매 제안                            │
│         │                                                                                    │
│         ▼                                                                                    │
│   ③ 리스크 가드레일 (결정론적 코드, 신규)  → 제안 재검증/축소 → 승인된 매매 액션              │
│         │                                                                                    │
│         ▼                                                                                    │
│   ④ 포트폴리오 원장 갱신 (portfolio.py, 신규) → git commit && git push                       │
└──────────────────────────────────────────┬───────────────────────────────────────────────────┘
                                              │  (GitHub 저장소에 커밋된 원장 파일)
                                              ▼
┌─────────────────────────────── Streamlit Cloud (조회 환경) ──────────────────────────────┐
│  누군가 대시보드를 열 때마다 — 매매 로직 없음, 원장을 읽어서 보여주기만 함                  │
│                                                                                              │
│   ⑤ 성과 리포팅 UI (pages/모의투자.py, 신규) — 최신 git 커밋 상태를 그대로 읽음            │
└──────────────────────────────────────────────────────────────────────────────────────────┘
```

①~④는 `scripts/run_daily_trading.py`(신규, 단일 진입점) 하나로 묶여서 GitHub Actions가 스케줄에 따라 실행한다. 이 스크립트는 로컬에서 수동으로도(`python scripts/run_daily_trading.py`) 실행 가능해야 테스트하기 쉽다. ⑤는 그 스크립트를 전혀 모른 채, git에 커밋된 parquet/json만 읽는다.

## 5. 컴포넌트 상세

### 5.1 가격예측 에이전트 (기존 확장)

- 기존 `predictor.train_and_predict()` / `screener.screen()` / `screener.top_movers()`를 그대로 재사용한다.
- 신규로 필요한 것: 워치리스트(예: 거래대금 상위 30~50종목, 기존 `top_movers` 결과 재사용) **전체**에 대해 반복적으로 `train_and_predict()`를 돌려 후보 리스트를 만드는 배치 함수 하나. 종목별 개별 조회가 아니라 이미 `screener.py`가 한 번에 가져온 스냅샷을 기반으로 순회하므로, 기존 "대량 조회는 벌크로" 규칙과 충돌하지 않는다 — 단, `predictor.train_and_predict()` 자체는 종목마다 별도 학습이 필요하므로 워치리스트 크기(30~50개)를 벗어나 전종목(2,500+)으로 확장하지 않는다.
- 산출물: `[{code, name, predicted_return_1d, predicted_return_5d, rsi14, news_sentiment, mae, directional_accuracy, ...}, ...]` — 이 중 예측 신뢰도 낮은 종목(`n_test`가 너무 적거나 `error` 반환)은 후보에서 제외.

### 5.2 매매 규칙 엔진 (신규, 결정론적 코드 — LLM 아님)

`src/trading_agent.py`의 `decide_trades()` — 5.1의 예측 신호 + 현재 보유 현황에 임계값 규칙을 적용해 매매 후보를 뽑는 순수 함수. **외부 API 호출이 전혀 없다** — 입력을 넣으면 항상 같은 출력이 나오는 결정론적 함수라, 실제 신호 데이터만 있으면 오프라인에서 바로 단위테스트할 수 있다(LLM 응답을 모킹할 필요 자체가 없어졌다).

**신규 진입(매수) 조건** — 5.1 후보 리스트의 각 종목이 아래를 **모두** 만족해야 매수 후보에 오른다:

```python
TRADING_RULES = {
    # 매수 조건
    "buy_return_threshold": 0.03,      # 예측 5거래일 수익률 +3% 이상
    "min_directional_accuracy": 0.55,  # 홀드아웃 방향 적중률 55% 이상 (예측 신뢰도 필터)
    "max_rsi_entry": 70,               # RSI 70 이하만 진입 — 과매수 구간 추격매수 방지
    "min_news_sentiment": -0.3,        # 뉴스 감성이 심하게 부정적이지 않을 것

    # 청산(매도) 조건 — 보유 종목에 적용
    "stop_loss_pct": -0.08,            # 평단가 대비 -8% 손절
    "take_profit_pct": 0.15,           # 평단가 대비 +15% 익절
    "max_rsi_exit": 80,                # RSI 80 이상이면 과매수 청산
    "exit_on_negative_signal": True,   # 예측 5일 수익률이 음전환되면 청산

    # 리스크 한도 — 6장과 완전히 동일한 값을 여기 한 곳에만 정의 (6장은 이 표를 옮겨 적은 것)
    "max_position_pct": 0.15,
    "max_holdings": 10,
    "min_trade_amount": 1_000_000,
    "max_daily_trades": 5,
    "min_cash_reserve_pct": 0.05,
}
```

**청산(매도) 조건** — 보유 종목마다 매 실행 시 확인, 하나라도 해당하면 매도:
1. 손절: 현재가가 평단가 대비 `stop_loss_pct` 이하
2. 익절: 현재가가 평단가 대비 `take_profit_pct` 이상
3. 신호 소멸: 그 종목의 예측 5일 수익률이 음수로 전환
4. RSI 과매수 청산: RSI가 `max_rsi_exit` 이상

**판단 순서**: 청산 조건부터 전수 확인 → 매수 후보를 예측 5일 수익률 내림차순 정렬 → `max_daily_trades`·`max_holdings` 한도 내에서 상위 후보 채택 → 각 매수 수량은 `max_position_pct`·`min_trade_amount`를 만족하는 선에서 계산.

**근거(reason) 문자열** — LLM 없이도 "왜 이 판단을 했는지" 투명하게 보여주는 것이 이 기능의 재미 포인트이므로, 조건을 그대로 문장으로 조립하는 템플릿을 쓴다 (자유 생성이 아니라 결정론적 포맷):

```python
f"예측 5일 수익률 {r5:+.1%}, 방향적중률 {acc:.0%}, RSI {rsi:.0f} — 매수 조건 충족"
f"평단가 대비 {pnl:+.1%} — 손절 기준({stop_loss_pct:.0%}) 도달로 매도"
f"평단가 대비 {pnl:+.1%} — 익절 기준({take_profit_pct:.0%}) 도달로 매도"
f"예측 수익률이 {r5:+.1%}로 음전환 — 신호 소멸로 매도"
```

**데이터 구조**: `pydantic` 없이 표준 라이브러리 `dataclasses.dataclass`로 `TradeAction`(action, code, quantity, reason)을 정의한다. 구조화된 출력을 강제할 LLM이 없으므로 pydantic이 검증하던 역할이 필요 없다 — **신규 의존성이 정말로 0개**가 된다.

### 5.3 리스크 가드레일 (신규, 결정론적 코드)

`decide_trades()`가 제안한 액션을 실제로 원장에 반영하기 전에, 별도의 순수 함수가 다시 한 번 검증한다. 5.2가 이미 같은 리스크 한도를 참조해서 판단하므로 원칙적으로 위반이 없어야 맞지만, **로직 자체의 버그(부호 반전, 임계값 실수, 여러 매수가 겹쳐 현금이 모자란 경우 등)에 대비한 두 번째 독립 방어선**으로 남겨둔다 — "판단 로직"과 "한도 강제 로직"을 분리해두면 각각을 따로 테스트할 수 있다는 이점도 있다.

- 매수 시 `quantity * 당일 종가` 합계가 가용 현금을 초과하면 해당 액션 거부(또는 가용 현금 내로 수량 축소)
- 매도 시 보유 수량을 초과하는 매도 거부
- 종목당 최대 비중, 동시 보유 종목 수, 하루 최대 거래 횟수 초과 시 거부 — 구체 수치는 6장
- 거부된 액션은 조용히 버리지 않고 사유와 함께 로그에 남겨 UI에서 "오늘 거부된 제안"으로 보여줄 수 있게 한다 (선택적, V1에서는 로그만 남기고 UI 노출은 생략 가능)

**체결가 가정**: V1은 실시간 시세가 없으므로(2장 참고), 그날 승인된 모든 매매는 **그날의 종가**로 체결된 것으로 가정한다. 이건 실제 장중 체결가와 다를 수 있지만, 일봉 기반 EOD 스윙 매매 시뮬레이션에서 표준적으로 쓰이는 단순화이며, 대시보드 어딘가(예: 거래내역 테이블 캡션)에 이 가정을 명시한다.

### 5.4 포트폴리오 원장 (신규)

`src/portfolio.py` — 아래 데이터를 관리하는 순수 데이터 계층. 실행 스크립트(5.5)와 조회 UI(5.6) 양쪽이 이 모듈을 통해서만 원장에 접근한다 (기존 `data_loader.py`/`screener.py` 패턴과 동일한 원칙 — 데이터 로직과 화면/실행 로직을 분리).

| 파일 | 내용 |
|---|---|
| `data/portfolio/state.json` | `cash`(현금), `last_run_date`(마지막 매매 실행일) |
| `data/portfolio/holdings.csv` | `code, name, quantity, avg_price` |
| `data/portfolio/trades.csv` | `date, code, name, action, quantity, price, amount, reason` (append-only 거래 이력) |
| `data/portfolio/equity_history.csv` | `date, cash, holdings_value, total_equity, kospi_close` (일별 자산 스냅샷 — 성과 차트용) |

**형식은 parquet이 아니라 CSV를 쓴다 — 이 프로젝트가 이미 겪은 문제 때문이다.** 다른 데이터 파일은 parquet인데 원장만 CSV인 것은 일관성을 깬 게 아니라, 용도가 다르기 때문이다:

- CLAUDE.md "알려진 제약"에 이미 기록돼 있듯, **parquet은 같은 내용을 다시 써도 바이트가 달라진다.** 그래서 git으로 추적하는 뉴스 감성 로그(`news.log_daily_sentiment()`)는 값이 그대로면 파일을 아예 건드리지 않는 `_already_logged()` 우회 로직을 따로 만들어야 했다 — 안 그러면 앱을 켤 때마다 워킹 트리가 dirty해지고 무의미한 diff 커밋이 쌓인다(실제로 쌓였던 이력이 있다). 원장도 parquet으로 하면 **같은 함정을 그대로 반복**하게 된다.
- CSV는 내용이 같으면 바이트도 같아서 그 우회 로직 자체가 필요 없고, git이 줄 단위 diff로 저장하므로 매일 커밋해도 저장소가 훨씬 덜 커진다(parquet은 매일 통짜 바이너리 blob이 하나씩 쌓인다).
- 원장은 1년 돌려도 수백 행 규모라 parquet의 압축·컬럼 조회 이점이 의미 없다. 반면 CSV는 GitHub 웹에서 그대로 열어 읽을 수 있어서, 유저가 "봇이 뭘 샀나"를 저장소에서 바로 확인할 수 있다는 실질적 이점이 있다.
- `.gitignore`에 `*.parquet`과 `*.csv`가 **둘 다** 있으므로, 어느 쪽을 쓰든 `!data/portfolio/` 예외 규칙을 `*.csv` 줄보다 **뒤에** 추가해야 한다 (마지막에 일치하는 규칙이 이긴다 — 기존 news_sentiment 예외와 같은 방식).

**시가평가(mark-to-market)는 매매와 분리된 별개의 매일 작업이다.** 매매가 0건인 날(규칙 엔진이 관망을 선택)에도 보유종목의 그날 종가는 바뀌므로 총자산은 변한다. 따라서 `equity_history.csv`는 **매 거래일 반드시 한 행씩 쌓인다** — "매매가 있었던 날만" 기록하면 자산 추이 차트에 구멍이 생기고 코스피 대비 성과 비교도 어긋난다. 실행 순서는 `시가평가 → 매매 판단 → 체결 반영 → 최종 시가평가 기록`이 아니라, **`매매 판단·체결 → 그날 종가로 전체 재평가 → equity 1행 기록`** 하나로 충분하다(체결도 종가 기준이므로 재평가를 두 번 할 이유가 없다).

**보유종목이 거래정지·상장폐지된 경우**: 그날 종가를 못 가져오는 종목은 마지막으로 알려진 가격으로 평가하고, 해당 사실을 UI에 표시한다. 매수 후보에서는 당연히 제외한다. (이 프로젝트는 이미 거래정지 종목 때문에 `predictor.py`가 크래시났던 이력이 있다 — 원장 쪽에서도 같은 데이터 특성을 전제하고 짠다.)

**영속성 전략 — 이 기능에서 가장 중요한 아키텍처 결정**:

Streamlit Community Cloud는 코드가 GitHub에서 재배포될 때마다 컨테이너를 새로 만들어, git에 커밋되지 않은 로컬 파일은 사라진다. 4장에서 정한 대로 매매 실행 주체가 **GitHub Actions**이므로, 해결책은 이 프로젝트가 이미 `data/raw/news_sentiment/*.parquet`(뉴스 감성 히스토리)에 쓰고 있는 것과 **동일한 패턴**이다: `data/portfolio/`도 `.gitignore` 예외로 추가하고(원장은 CSV이므로 `*.csv` 줄보다 뒤에 와야 한다 — 위 형식 논의 참고), **GitHub Actions 워크플로 자신이** 매매 실행 후 `git add && git commit && git push`까지 수행해 저장소에 상태를 기록한다.

- GitHub Actions는 저장소 안에서 실행되는 워크플로에 한해 **자체 `GITHUB_TOKEN`을 기본 제공**하므로, 워크플로 YAML에 `permissions: contents: write`만 주면 별도로 PAT를 발급해서 시크릿에 저장할 필요가 없다 — v1 초안에서 "실행 중인 Streamlit 앱이 자기 저장소에 PAT로 푸시한다"고 짰던 것보다 훨씬 단순하고 안전하다 (Streamlit Cloud 쪽엔 이제 쓰기 권한이 있는 토큰이 아예 존재하지 않는다).
- **매매 판단이 규칙 기반으로 바뀌면서 시크릿 자체가 필요 없어졌다.** 원래는 LLM 호출용 `ANTHROPIC_API_KEY`를 GitHub Actions Secrets에 등록해야 했지만(9장에 원안 보존), 지금 설계엔 외부 API 호출이 없으므로 이 워크플로에 필요한 시크릿은 **0개**다. Streamlit Cloud Secrets에도 여전히 `DART_API_KEY`만 있으면 된다.
- 쓰기 주체가 GitHub Actions 하나뿐이라 여러 Streamlit 방문자가 동시에 커밋을 유발하는 경합 상태는 애초에 발생하지 않는다. 다만 같은 스케줄이 중복 실행되는 것(수동 재실행 등)은 워크플로의 `concurrency:` 설정과 스크립트 내부의 `last_run_date` 검사(5.5) 두 겹으로 막는다.
- 대안(Supabase/Turso 같은 외부 DB)은 더 견고하지만 이 프로젝트에 새 외부 의존성을 추가하는 것이므로, git-커밋 방식이 실제로 문제(용량 증가, 커밋 이력 비대화 등)를 일으킬 때 넘어가는 것으로 미룬다 (9장 향후 확장 참고).

### 5.5 실행 스케줄러 — GitHub Actions (신규)

`.github/workflows/daily_trading.yml` — 방문자 유무와 완전히 무관하게, 정해진 시각에 자동으로 `scripts/run_daily_trading.py`를 실행한다.

```yaml
name: 모의투자 일일 매매
on:
  schedule:
    - cron: "30 10 * * 1-5"   # 매 평일 10:30 UTC = 19:30 KST — 아래 실측 근거 참고
  workflow_dispatch: {}        # 수동 재실행 버튼 (테스트/장애 대응용)

concurrency:
  group: daily-trading
  cancel-in-progress: false  # 겹쳐 실행되지 않도록 순서대로 대기

permissions:
  contents: write             # 이 토큰으로 git push까지 수행

jobs:
  trade:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"      # Actions 러너가 3.14를 지원하는지 구현 시 확인 필요
      - run: pip install -r requirements-trading.txt   # 전체 requirements.txt 아님 — 아래 참고
      - run: python scripts/run_daily_trading.py        # 외부 API 키 불필요 — env 섹션 자체가 없다
      - run: |
          git config user.name "모의투자 봇"
          git config user.email "actions@github.com"
          git add data/portfolio/
          git diff --cached --quiet || git commit -m "모의투자: $(date +%Y-%m-%d) 매매 결과 반영"
          git push
```

**의존성 — 신규 패키지가 없다.** 규칙 기반 엔진(5.2)은 `dataclasses`(표준 라이브러리)만 쓰고 외부 API 클라이언트가 없으므로, 매매 스크립트에 필요한 패키지는 전부 이미 `requirements.txt`에 있다. 다만 `streamlit`·`jupyterlab`·`matplotlib`는 매매 스크립트에 전혀 필요 없는데 `requirements.txt` 전체를 설치하면 매일 Actions에서 수 분씩 낭비된다(jupyterlab이 특히 무겁다) — 그래서 실행에 실제로 쓰는 것만 추린 `requirements-trading.txt`를 따로 둔다: `finance-datareader`, `pandas`, `numpy`, `pyarrow`, `scikit-learn`, `requests`, `lxml`, `beautifulsoup4`, `python-dotenv`. (버전 핀은 기존 `requirements.txt`와 동일하게 유지해서 두 환경이 어긋나지 않게 한다.) 0단계에서 설치 확인했던 `anthropic`·`pydantic`은 LLM 경로를 나중에 켤 때(9장) 다시 쓴다.

**타이밍 — 실측 완료 (2026-08-13, GitHub 커밋 이력 기반)**:

`screener.py`는 KRX를 직접 치는 게 아니라 **FinanceDataReader의 GitHub 미러**(`FinanceData/fdr_krx_data_cache`의 일자별 스냅샷 CSV — `CLAUDE.md` "알려진 제약" 참고)에서 그날 데이터를 가져온다. 이 미러가 몇 시에 그날치로 확정되는지는 문서로 보장되지 않으므로, GitHub API로 최근 완료된 거래일 4일치(`data/listing/krx/{날짜}.csv`)의 **커밋 이력**을 직접 조회해 확인했다.

| 날짜(거래일) | 최초 커밋(UTC) | **최종(확정) 커밋** | KST 환산 | 총 커밋 수 |
|---|---|---|---|---|
| 2026-08-07(금) | 02:11:52 | 09:36:14 | **18:36 KST** | 6 |
| 2026-08-10(월) | (전날)16:29:35 | 09:49:46 | **18:49 KST** | 7 |
| 2026-08-11(화) | (전날)16:50:12 | 09:15:21 | **18:15 KST** | 7 |
| 2026-08-12(수) | (전날)16:54:03 | 09:32:57 | **18:32 KST** | 7 |

**발견한 것**: 파일은 KST 새벽 1~2시경 빈 스텁으로 먼저 생기고, 장중 여러 차례(하루 6~7커밋) 갱신되다가, **최종 확정 커밋은 18:15~18:49 KST 사이**에 온다 — 애초 가정했던 "17:00 KST"는 **틀렸다**. 그 시각에 돌렸으면 매일 전날 데이터로 매매했을 것이다.

- **cron을 19:30 KST(=10:30 UTC)로 잡는다** — 관측된 확정 구간(18:15~18:49)보다 40분~1시간 15분 여유를 둔 값. 위 워크플로 예시에 반영했다.
- 표본이 4거래일뿐이라 이보다 늦게 확정되는 날이 있을 수 있다. 그래서 스크립트는 **오늘 날짜 스냅샷이 아직 없거나, 있어도 방금 막 생긴 스텁으로 의심되면 아무 것도 하지 않고 정상 종료**하도록 방어적으로 짠다 (에러로 죽지 않는다 — 죽으면 Actions 실패 알림만 쌓인다).
- 배포 후 첫 2~3주는 실제로 19:30 KST에 그날 데이터가 잡히는지 계속 관찰하고, 필요하면 cron을 더 늦춘다 (10장 6단계).

**휴장일**: GitHub Actions `cron`은 한국 공휴일을 모른다. 주말은 `1-5`로 걸렀지만 평일 공휴일(설·추석 등)엔 워크플로가 그대로 실행된다 — 이때도 위와 같은 이유로 "오늘 스냅샷 없음 → 조용히 종료"로 자연스럽게 처리된다. 공휴일 달력을 따로 관리할 필요가 없다는 뜻이다.

**실패 처리 — 원장이 반쯤 갱신된 상태로 남지 않게 한다**:
- `state.json`의 `last_run_date`는 **모든 작업이 성공적으로 끝난 뒤 마지막에** 갱신한다. 네트워크 오류(KRX 스냅샷·개별 종목 시세 조회 실패) 등으로 중간에 실패하면 `last_run_date`가 그대로 남아, 다음 스케줄(또는 수동 재실행)이 "오늘 아직 안 돌았다"고 판단해 다시 시도할 수 있다.
- 반대로 매매를 체결해놓고 `last_run_date`만 못 쓰면 **다음 실행이 같은 날 매매를 두 번 하게 된다.** 이를 막기 위해 원장 4개 파일을 모두 메모리에서 갱신한 뒤 **한 번에 파일로 쓰고**, 그 직후 `last_run_date`를 쓴다. 부분 실패 시엔 아무 파일도 안 쓴 상태가 되게 한다.
- 커밋도 마찬가지로 **전부 성공한 경우에만** 한다 (`git add data/portfolio/`가 4개 파일을 한 커밋으로 묶으므로, 파일 쓰기가 원자적이면 커밋도 원자적이다).
- 규칙 엔진 자체는 외부 API가 없어 실패할 일이 거의 없지만, ①(가격예측)이 쓰는 시세 조회는 여전히 네트워크 의존적이다. 실패하면 **그날은 매매 없이 시가평가만 기록**하고 종료하는 것을 권장한다 — 자산 추이 차트에 구멍을 내지 않기 위해서다.

**로컬 수동 실행과의 충돌**: `scripts/run_daily_trading.py`를 로컬에서 돌려 커밋한 뒤 push하지 않으면, 다음 Actions 실행이 원격 기준으로 돌아 이력이 갈라진다. 로컬 실행은 개발/디버깅 용도로만 쓰고, **원장을 실제로 갱신하는 실행은 Actions 한 곳으로 일원화**한다 (로컬 테스트 시엔 `data/portfolio/`를 건드리지 않는 dry-run 옵션을 두는 것을 권장).

### 5.6 성과 리포팅 UI (신규)

**배치 방식 (사용자 확정)**: 기존 `app.py`는 지금 그대로 지인에게 공유 중인 조회용 대시보드이므로 **손대지 않는다.** 대신 기존 기능 + 모의투자를 함께 묶은 **새 "데모" 대시보드**를 별도로 만든다.

구현은 `st.navigation` + `st.Page`(Streamlit 1.36+, 이 프로젝트의 1.61.1에 이미 포함)를 쓴다 — 이 API는 스크립트 경로를 그대로 페이지로 등록할 수 있어서, **`app.py`를 한 글자도 수정하지 않고** 기존 페이지로 재사용할 수 있다.

```python
# demo_app.py (신규, 새 진입점)
import streamlit as st

pg = st.navigation([
    st.Page("app.py", title="주가 스크리닝", icon="📊"),
    st.Page("pages/모의투자.py", title="모의투자", icon="💰"),
])
pg.run()
```

- `app.py`는 기존 배포(현재 공유 중인 링크)에서 계속 단독으로도 동작한다 — `demo_app.py`는 그 위에 얹는 것이지 대체가 아니다.
- Streamlit Cloud에 **새 앱**으로 하나 더 배포(main file path를 `demo_app.py`로 지정)하면 기존 링크는 그대로 살아있고, 데모용 링크가 하나 더 생긴다. 두 배포가 같은 저장소·같은 `data/portfolio/` 원장을 공유하므로 추가 설정은 필요 없다.
- 이 페이지(`pages/모의투자.py`, 데모 앱 전용)는 **원장을 읽기만** 하며, 매매를 유발하는 코드를 절대 포함하지 않는다 (4장의 실행/조회 분리 원칙).

`pages/모의투자.py` 구성안:
- 상단: 총자산(현금+평가금액), 오늘 기준 누적 수익률, 코스피 지수 대비 초과수익률, **마지막 매매 실행 일시**(원장이 언제 기준인지 — "오늘 장중 실시간 아님, 어제 마감 기준" 같은 캡션과 함께)
- 자산 추이 차트: `equity_history.csv` 기반 라인 차트 (총자산 vs 코스피 리베이스 비교 — 기존 `charts._rebase` 재사용)
- 보유종목 테이블: 종목명, 수량, 평단가, 최근 종가, 평가손익률
- 거래 내역 테이블: 최근 거래(날짜, 종목, 매수/매도, 수량, 체결가, **판단 근거**) — `reason` 필드를 노출해서 "왜 이 판단을 했는지" 투명하게 보여주는 것이 이 기능의 재미 포인트 (5.2의 규칙 템플릿으로 생성된 문장)
- 하단: "오늘의 판단 요약" — LLM 문단이 아니라 그날 결과를 조립한 템플릿 문장, 예: `f"오늘 매수 {n_buy}건, 매도 {n_sell}건. 총자산 {total:,.0f}원 ({daily_return:+.2%})"`

## 6. 리스크 관리 정책 (기본값 제안 — 구현 시 상수로 빼서 조정 가능하게)

| 규칙 | 기본값 |
|---|---|
| 종목당 최대 투자 비중 | 총자산의 15% |
| 동시 보유 종목 수 한도 | 10종목 |
| 1회 매매당 최소 금액 | 100만원 (너무 잘게 쪼개는 것 방지) |
| 하루 최대 거래 횟수 | 5건 |
| 현금 최소 보유 비율 | 총자산의 5% (전액 소진 방지) |

이 표는 5.2의 `TRADING_RULES` 딕셔너리와 정확히 같은 값이다 — 매매 판단(5.2)과 최종 한도 검증(5.3)이 같은 상수 하나를 공유하므로, 예전(LLM 안) 설계에서 걱정했던 "판단 쪽과 검증 쪽 숫자가 따로 하드코딩돼 어긋나는" 문제는 애초에 생기지 않는다.

## 7. 면책 조항 (기존 `predictor.py` 규칙의 연장)

기존 `CLAUDE.md`의 "가격 예측(ML) 기능 작성 규칙"이 이미 "투자 조언처럼 보이지 않게 한다"를 못박고 있는데, 모의투자는 실제로 "매수/매도"를 자동으로 **실행**하는 기능이라 이 원칙이 더 중요해진다.

- 화면 전체에 "실제 금전 거래가 아닌 시뮬레이션 게임입니다. 투자 조언이 아닙니다" 문구를 상시 노출 (기존 예측 경고 문구보다 더 눈에 띄게).
- "오늘 장중 실시간으로 움직이는 게 아니라, 하루 1회 장 마감 후 자동 매매된 결과"라는 점을 명시 (2장에서 정리한 스코프 결정과 일치시켜, 유저가 실시간 트레이딩으로 오해하지 않게 한다).
- 거래 근거(`reason`)는 어디까지나 정해진 규칙(임계값)이 그대로 조립된 문장이지, 사용자에게 그 종목을 사라고 권유하는 것이 아님을 문구로 분명히 한다.
- 규칙 엔진은 결정론적이라 "정책을 위반하는 판단"이 원칙적으로 나올 수 없지만(같은 상수를 참조하므로), 로직 버그 가능성은 항상 있다. 5.3의 가드레일이 이를 한 번 더 걸러낸다는 점을 신뢰하되, 정기적으로 `trades.csv`를 눈으로 검토하는 것을 권장한다 (CSV이므로 GitHub 웹에서 바로 열어볼 수 있다).

## 8. 비용 추정

규칙 기반 엔진으로 전환하면서 **외부 API 비용이 완전히 사라졌다** — Claude API 키 발급·과금·배치를 지금 단계에서는 전혀 신경 쓸 필요가 없다 (이전에 있었던 API 키 발급 안내·토큰 사용량 추정은 9장 "LLM 기반 전환(보류)"에 참고용으로 옮겨뒀다).

**GitHub Actions 실행 비용**: 하루 1회, 종속성 설치 포함 몇 분 이내로 예상. 이 저장소가 private이면 무료 플랜 기준 월 2,000분까지 무료인데(퍼블릭 저장소는 무제한 무료), 하루 5분 잡아도 월 110분 수준이라 무료 한도 안에서 여유 있게 돈다.

**V1 총 운영 비용: $0.** (GitHub Actions 무료 한도 안, 새 유료 API 없음)

## 9. 향후 확장 (V1 이후)

### 9.1 LLM 기반 전환 (원래 설계, 보류)

원래 설계는 규칙 임계값이 아니라 **Claude API 기반 판단형 에이전트**였다 — 예측치·기술지표·뉴스감성·현재 포지션을 종합해 Claude가 매수/매도/보유를 판단하고 한국어 근거를 남기는 방식. "유료 서비스라 지금은 키 발급 없이 진행"하는 사용자 결정으로 V1에서는 규칙 엔진(5.2)으로 대체했지만, **인터페이스 경계를 그대로 유지**해뒀으므로 나중에 전환하고 싶으면 `trading_agent.py`의 `decide_trades()` 함수 내부 구현만 규칙 엔진→LLM 호출로 바꾸면 된다(호출부인 `scripts/run_daily_trading.py`나 5.3 가드레일, 5.4 원장, 5.6 UI는 손댈 필요가 없다 — 가드레일이 "제안을 다시 검증"하는 구조이므로 제안이 규칙에서 나오든 LLM에서 나오든 동일하게 동작한다).

키가 준비되면 되살릴 수 있도록 원안을 남겨둔다:

- **모델**: `claude-opus-5` 기본값. 비용에 민감하면 `claude-sonnet-5`.
- **입력**: 포트폴리오 상태, 오늘의 후보 리스트(5.1), 최근 거래 이력, 리스크 정책(6장) — "이 규칙 안에서 판단하라"는 형태로 프롬프트에 명시하되, 최종 강제는 5.3(코드)이 담당.
- **출력**: 구조화된 출력(`client.messages.parse()` + Pydantic)으로 강제.
  ```python
  class TradeAction(BaseModel):
      action: Literal["buy", "sell"]
      code: str
      quantity: int
      reason: str

  class TradingDecision(BaseModel):
      actions: list[TradeAction]
      summary: str
  ```
- **API 키 배치**: 로컬 개발은 `.env`의 `ANTHROPIC_API_KEY`(DART_API_KEY와 같은 파일), 운영은 GitHub 저장소 Settings → Secrets and variables → Actions에 `ANTHROPIC_API_KEY`로 등록(워크플로 5.5의 `env:`에서 읽음). Streamlit Cloud Secrets에는 두지 않는다(LLM 호출이 GitHub Actions 안에서만 일어나므로).
- **예상 비용** (구성 요소별 토큰 추정 — 실제 프롬프트 작성 전 추론값): 입력 약 2,150~3,200 토큰(시스템프롬프트 600~800 + 포트폴리오 200~250 + 후보 30~50종목 1,000~1,600 + 거래이력 150~250 + 리스크정책 100~150 + 지시문 100~150), 출력 약 130~600 토큰(액션 0~5건 + 요약). `claude-opus-5` 기준 호출 1회 $0.015~0.03, **월간(거래일 22일) 약 $0.35~0.65**(500~900원) — 개인 프로젝트 규모에서 사실상 무시할 수 있는 금액이지만, 그래도 키 발급·결제수단 등록이라는 절차 자체가 지금은 부담이라 미룬 것.
- 전환 시에도 장중 반복 호출은 하지 않는다 — 아래 9.2와 같은 이유.

### 9.2 장중 실시간 모니터링

실시간 시세 데이터 소스(증권사 Open API 계좌 또는 유료 벤더)를 확보하면, 보유종목 손절/익절을 장중에 체크하고 평가금액을 실시간에 가깝게 갱신하는 2번째 스케줄(예: 30분 간격)을 추가할 수 있다. 이때도 **신규 매수/매도 판단은 여전히 하루 1회**로 유지하고, 장중 스케줄은 이미 보유한 포지션의 결정론적 리스크 체크(5.3 가드레일 재사용)만 담당하는 방향을 권장한다 — 규칙 엔진이든 LLM이든 판단 로직을 30분마다 다시 도는 건 새 정보(그날 종가는 아직 안 나왔으므로) 없이 리소스만 쓰는 것이기 때문이다.

### 9.3 그 외

- **백테스트**: 실전 투입 전 과거 데이터로 전략을 시뮬레이션해서 명백히 나쁜 전략(예: 상시 손실)이 아닌지 검증. 규칙 엔진은 결정론적이라 과거 데이터에 그대로 재적용하기 쉽다 — LLM 버전보다 오히려 백테스트가 간단해졌다.
- **멀티유저 포트폴리오**: 방문자별 개인 가상현금 — 인증 체계와 사용자별 데이터 분리가 필요한 큰 확장이라 별도 PRD로 분리 권장.
- **외부 DB로 원장 이전**: git-커밋 방식이 실무에서 병목/충돌을 일으키면 Supabase 등으로 이전.

## 10. 구현 단계 제안

**0단계는 코딩이 아니라 실측이다.** 아래 1~6은 순서를 지키는 편이 좋고, 특히 0단계는 뒤에서 하면 스케줄을 다시 짜야 할 수 있으므로 먼저 한다.

0. ~~KRX 스냅샷 갱신 시각 실측 + 패키지 스모크 테스트~~ **완료 (2026-08-13)**.
   - KRX 스냅샷 확정 시각: 18:15~18:49 KST로 확인, cron을 19:30 KST로 확정 (5.5 실측 표 참고).
   - `anthropic==0.121.0`, `pydantic==2.13.4` 설치까지 확인했었으나, **매매 판단을 규칙 기반으로
     전환하면서 이 두 패키지는 V1에서 쓰지 않는다** (9.1 LLM 전환 시 재사용). requirements에도
     추가하지 않는다 — 5.2가 표준 라이브러리 `dataclasses`만 쓴다.
1. ~~`src/portfolio.py` — 원장 데이터 모델 + 읽기/쓰기(CSV) + 시가평가 함수~~ **완료 (2026-08-13)**.
   git 커밋 로직은 아직 붙이지 않고 로컬 파일로만 개발/테스트했다. 테스트 18개, CSV 바이트 안정성
   회귀테스트 포함, `ruff` 통과.
2. ~~`src/trading_agent.py` — `decide_trades()` 규칙 엔진(5.2) + 리스크 가드레일(5.3)~~ **완료 (2026-08-13)**.
   외부 API가 없어 모킹 불필요 — 합성 신호 데이터로 각 조건의 경계값(임계값 바로 위/아래)을
   촘촘히 단위테스트(35개)했고, 가드레일이 "규칙 엔진 자체의 버그로 생긴" 한도 초과 제안을
   실제로 거부하는지 집중 검증했다. `decide_trades()` → `apply_risk_guardrail()` →
   `portfolio.apply_trade()` 연계까지 수동 통합 시나리오로 확인.
3. ~~`scripts/run_daily_trading.py` — 5.1~5.4를 묶는 단일 진입점 + dry-run 옵션 + 실패 시 원장 미갱신(5.5)~~ **완료 (2026-08-13)**.
   `--dry-run`은 원장을 안 건드리고 판단 결과만 출력. `last_run_date`가 오늘이면 조기 종료,
   오늘 KRX 스냅샷이 없으면(`screener.screen()`이 `RuntimeError`) 매매 없이 조용히 종료. 워치리스트
   (거래대금 상위 40 + 보유종목)를 순회하며 종목별 신호 생성이 실패해도 그 종목만 스킵하고 계속
   진행(개별 종목 예외가 전체 실행을 죽이지 않음). 워치리스트 3종목으로 축소해 실제
   네트워크(`screener`·`predictor`·`news`)를 태운 `--dry-run` 스모크 테스트로 동작 확인.
   원장 갱신은 `portfolio.save_daily_result()` 한 번 호출로 위임하므로 5.4의 원자적 쓰기 순서를
   그대로 물려받는다.
4. ~~`pages/모의투자.py` — 원장을 읽어 보여주는 UI~~ **완료 (2026-08-14)**.
   `demo_app.py`(신규, `st.navigation` + `st.Page`)도 같이 만들어 `app.py`를 한 글자도 안 건드리고
   기존 페이지로 재사용했다 — `st.set_page_config()`를 `demo_app.py`와 `app.py` 양쪽이 각각
   호출하지만(둘 다 손댈 수 없거나 손대지 않기로 한 코드), `st.navigation`으로 페이지 전환 시
   에러가 나지 않는 것을 AppTest로 실측 확인했다(Streamlit 1.61.1). 보유종목 "최근 종가"는
   종목별 반복 조회가 아니라 `screener.screen()` 벌크 스냅샷 한 번으로 채운다(벌크 조회 규칙).
   자산 추이 차트는 `equity_history.csv`의 `total_equity`/`kospi_close`를 같은 날짜 기준으로
   시작값=100 리베이스해서 겹쳐 그린다(두 컬럼이 이미 같은 날짜 인덱스를 공유하므로
   `charts._rebase`의 reindex/ffill 기능 없이 직접 정규화). AppTest로 (a) 초기 상태(현금 1억,
   무보유, 거래이력 없음)와 (b) 보유종목·거래내역·자산추이가 있는 합성 상태 양쪽 모두 예외 없이
   렌더링되는 것을 확인했고, `streamlit run demo_app.py`를 8502 포트로 띄워 실제 서버가 에러 없이
   기동하는 것도 확인했다(Chrome 브라우저 도구가 이번 세션에 비활성화돼 있어 실제 화면 스크린샷
   확인은 못 했다 — AppTest의 엘리먼트 트리 검사로 대체).
5. ~~`.github/workflows/daily_trading.yml` + `requirements-trading.txt` — 스케줄러 연결~~ **완료
   (2026-08-14), `workflow_dispatch` 수동 실행으로 실전 검증까지 마쳤다.** 진행 중 실제로 겪은
   버그와 수정:
   - **`ModuleNotFoundError: matplotlib`**: `src/__init__.py`가 모든 서브모듈을 eager import하고
     있어서 `from src import data_loader`만 해도 `plotting.py`(matplotlib)까지 끌려 들어왔다.
     `requirements-trading.txt`는 matplotlib을 의도적으로 뺐으므로 최초 import 단계에서 죽었다.
     호출부가 이미 전부 명시적 서브모듈 import를 쓰므로 `__init__.py`의 eager import를 제거해
     해결(커밋 `8d789f3`). 격리된 venv(트레이딩 의존성만 설치)로 재현·검증.
   - **"Re-run jobs"의 함정**: 수정 커밋을 push한 뒤에도 GitHub UI에서 실패한 run을 "Re-run
     jobs"로 재시도하면 **그 run이 원래 트리거됐던 시점의 커밋을 그대로 재사용**해서 새 커밋이
     반영되지 않는다(같은 에러가 반복돼서 헷갈렸던 지점). 새 커밋을 반영하려면 Actions 탭에서
     **"Run workflow"**로 완전히 새 run을 만들어야 한다.
   - **최초 실전 실행 결과(2026-08-14, 커밋 `c0f667f`)**: 워치리스트 40종목 중 37개 신호 생성
     (2종목은 네이버 뉴스 타임아웃 — 설계대로 해당 종목만 스킵하고 계속 진행), 매수 4건 체결,
     원장 커밋·푸시 정상.
   - **`schedule` 활성화 완료 (2026-08-14, 커밋 예정)**: 사용자 승인 후 `cron: "30 10 * * 1-5"`
     주석을 해제했다. `workflow_dispatch`는 재검증/디버깅용으로 계속 남겨둔다. 이제부터는
     PRD 10장 6단계(며칠 관찰 — 영속성·타이밍·휴장일 스킵·Streamlit 반영 확인)로 넘어간다.
   - `requirements-trading.txt`는 `scripts/run_daily_trading.py`의 전체 import 체인을 직접
     추적해서 만들었고, 버전은 `requirements.txt`와 동일하게 고정(이미 이 환경에서 검증된 조합).
     신규 시크릿은 없다(V1은 규칙 기반이라 `ANTHROPIC_API_KEY` 불필요, `permissions:
     contents: write`로 기본 `GITHUB_TOKEN`만으로 커밋·푸시).
6. 실제 배포 환경에서 며칠 관찰 — 영속성(커밋이 실제로 쌓이는지), 타이밍(0단계 실측대로 맞는지), 휴장일 스킵, Streamlit 쪽에서 갱신된 원장이 보이는지. 로컬에서는 검증 불가능한 부분이므로 이 단계를 건너뛰지 않는다.
7. **투자 풀 확장 — 국내 전용에서 국내+해외증시(나스닥)+코인(업비트)로 (2026-08-14).** 사용자가 확정한 세 가지 아키텍처 결정:
   - **원장은 단일 원화(KRW) 하나로 통합.** 자산별 하위 원장(달러 잔액 등)을 따로 두지 않는다 — 해외증시는 매수/매도 시점 환율로 원화 환산해서 이후 파이프라인(현금 확인·포지션 한도·최소거래금액)에 넣고, 코인은 업비트가 이미 원화 시세라 환산이 필요 없다. `data_loader.get_usdkrw_rate()`(신규, `pages/모니터링.py`의 통화 토글과 공유)로 환산한다.
   - **`TRADING_RULES`(손절/익절/RSI 등)는 세 시장에 공통 적용.** 자산군별 별도 규칙 세트를 두지 않는다.
   - **보유 한도(종목당 최대 비중 15%, 동시보유 10종목)도 세 시장 통합 기준.** 시장별 서브 한도는 두지 않는다.

   원장 스키마(`holdings.csv`/`trades.csv`)는 **바꾸지 않았다** — market 컬럼을 추가하는 마이그레이션은 라이브 원장에 리스크가 있어서, 대신 `trading_agent.infer_market(code)`가 종목코드 문자열 패턴(6자리 숫자=KR, `"KRW-"` 접두사=COIN, 그 외=US)만으로 시장을 판별한다. 세 패턴은 서로 겹치지 않는다.

   `scripts/run_daily_trading.py`의 워치리스트를 국내(거래대금 상위 40, 기존과 동일) + 해외증시(`us_screener.screen()` 상위 15) + 코인(`crypto_screener.screen()` 상위 15)으로 넓혔다 — 해외/코인은 신규 시장이라 국내보다 작게 시작했다(예측 학습 + 뉴스 조회 비용이 후보 수만큼 그대로 곱해지므로, 안정성을 지켜본 뒤 값만 올리면 된다). **국내(KRX) 스냅샷 실패는 기존대로 전체 실행을 중단하지만, 해외증시·코인 스냅샷 실패는 그 시장 후보만 빼고 국내 매매는 계속 진행한다** — 신규 시장의 API 불안정이 이미 검증된 국내 매매까지 막지 않게 하려는 의도적 비대칭이다.

   **코인 수량은 정수 주가 아니라 소수(8자리 반올림)로 계산한다** — 비트코인처럼 1개 가격이 종목당 최대 비중 예산(예: 1,500만원)보다 훨씬 큰 코인은 정수 수량 로직으로는 `int(15_000_000 // 150_000_000) == 0`이 되어 매수가 원천적으로 불가능해진다. `TradeAction.quantity` 타입을 `int | float`로 넓히고, `decide_trades()`가 `infer_market(code) == "COIN"`일 때만 소수 계산 경로를 탄다.

   **구현 중 실제로 잡은 버그 2건**(둘 다 "int() 캐스팅이 1 미만 소수 코인 보유분을 0으로 잘라버리는" 같은 원인의 다른 증상): (1) `decide_trades()`의 청산(매도) 루프가 매도 수량을 `int(row["quantity"])`로 캐스팅해서, 1 미만 코인 보유분(예: 0.5 BTC)의 손절/익절 매도 제안이 `quantity=0`이 되어 `portfolio.apply_trade()`의 "quantity는 양수여야 합니다" 예외로 그날 실행 전체가 죽는 문제. (2) `apply_risk_guardrail()`의 `sim_holdings` 초기화가 같은 이유로 소수 보유분을 0으로 봐서, 실제로는 다 갖고 있는데도 "보유 수량을 초과하는 매도 제안"으로 잘못 거부하는 문제. 둘 다 보유 수량을 그대로(코인이면 float) 쓰도록 고쳤고, 회귀 테스트를 추가했다.

   검증: `tests/test_trading_agent.py` 오프라인 테스트(43개, `infer_market()`·코인 소수 수량·위 회귀 테스트 포함), 실제 라이브 API(`us_screener`/`crypto_screener`/`data_loader.get_usdkrw_rate()`/`predictor`/`news`)를 태운 `--dry-run` 스모크 테스트로 워치리스트 70종목(국내 40+해외 15+코인 15) 전체가 예외 없이 처리되고, 해외증시 원화 환산가(AAPL $305.26 → 432,749원)가 `pages/모니터링.py` 통화 토글의 계산 결과와 정확히 일치하는 것까지 확인. `pages/모의투자.py`(성과 리포팅 UI)는 이미 모든 금액을 "원" 단위로만 표시하고 수량에 정수 포맷을 강제하는 곳이 없어 **변경 없이 그대로 동작한다**(단일 원화 원장 설계 덕분).

8. **봇 다중화 — 기본형/공격적/모멘텀 (2026-08-22).** 사용자가 서로 다른 매매 전략을 가진
   봇 여러 개를 같은 날부터 나란히 돌려 성과를 비교하고 싶다는 요청에 따라 확장. 확정된
   방향은 "임계값만 다른 게 아니라 판단 로직 자체가 분리된 별도 함수"였다.

   **아키텍처**: 시세 스냅샷·예측 신호·뉴스 감성(계산 비용이 큼)은 세 봇이 하루 한 번
   계산한 결과를 공유하고, 그 신호로 "무엇을 살지" 판단하는 함수만 봇마다 다르다.
   - `decide_trades`(기본형, 무수정) — 예측 5일 수익률 임계값 + 방향적중률 + RSI + 뉴스감성
     필터. 신규 진입만, 보유 종목은 무조건 신규 매수 후보에서 제외.
   - `decide_trades_aggressive`(신규) — 임계값을 낮추고 손절/익절 폭·포지션 크기를 키운다.
     구조적으로 다른 지점: **이미 보유한 종목도 추가 매수(피라미딩) 후보에 포함**한다(단,
     종목당 최대 비중을 넘지 않는 만큼만). 동시 보유 종목 수 한도는 신규 진입에만 적용되고
     추가매수는 안 걸린다(기존 `apply_risk_guardrail()`의 해석과 동일하게 맞춤).
   - `decide_trades_momentum`(신규) — 진입 1차 조건이 예측 수익률 크기가 아니라 **추세
     정렬**(종가가 SMA5·SMA20 위 — `run_daily_trading._build_signal()`이 `indicators.sma()`로
     계산해 신호에 `sma5_gap`/`sma20_gap` 필드로 추가, 신규 네트워크 호출 없음)이다. 청산도
     기존 4조건에 **추세 이탈**(sma20_gap 음전환) 조건이 하나 더 붙는다. RSI 상한은 오히려
     완화(추세 추종은 과매수 구간에서도 진입 허용).
   - 가드레일(`apply_risk_guardrail`)은 전략이 아니라 한도 검증이라 세 봇이 그대로 공유한다.
   - `trading_agent.BOT_STRATEGIES`(단일 레지스트리, `{bot_id: {label, decide_trades, rules}}`)에
     세 봇을 등록 — `run_daily_trading.py`/`pages/모의투자.py`가 이 딕셔너리 하나만 순회한다.

   **원장 분리**: `config.portfolio_dir_for(bot_id)` — "default"는 기존 `data/portfolio/`
   그대로(라이브 원장이라 마이그레이션 없음), 그 외는 `data/portfolio/{bot_id}/`에 완전히
   새 원장(1억원)으로 시작한다. `portfolio.py`의 모든 읽기/쓰기 함수가 `portfolio_dir:
   Path | None = None` 파라미터를 받는다 — `None`이면 기존 모듈 상수(`_STATE_PATH` 등)를
   그대로 참조하므로 `tests/test_portfolio.py`의 기존 monkeypatch 테스트가 무수정으로 통과한다
   (기본형 봇 하위호환 검증 자체가 테스트).

   **`.gitignore` 버그 발견·수정**: `!data/portfolio/*.csv`는 한 단계 깊이만 un-ignore해서
   `data/portfolio/aggressive/holdings.csv`처럼 하위 디렉터리의 CSV는 여전히 전역 `*.csv`
   규칙에 걸려 GitHub Actions가 매일 커밋을 시도해도 조용히 아무것도 안 올라가는 상태가
   될 뻔했다 — `!data/portfolio/**/*.csv`로 재귀 매치하도록 고쳤다(`git add --dry-run`으로
   실제 추가 대상이 되는지 직접 확인).

   `scripts/run_daily_trading.py`는 `BOT_STRATEGIES`를 순회하며 봇마다 `holdings`/`cash`/
   `rules`/`decide_trades`로 판단→가드레일→체결→시가평가→원장 반영까지 독립적으로
   완결한다(한 봇의 예외가 다른 봇을 막지 않게 봇 단위 try/except). `pages/모의투자.py`는
   봇 탭(기본형/공격적/모멘텀) + "성과 비교" 탭(세 봇 요약 표 + 자산 추이 오버레이 차트)으로
   재구성했다.

   검증: `tests/test_trading_agent.py`(공격적/모멘텀 경계값 테스트 추가), `tests/
   test_portfolio.py`(다중 원장 격리 테스트 추가) 오프라인 전체 통과, 워치리스트를 4종목으로
   줄인 `--dry-run` 스모크 테스트로 세 봇 모두 실제 라이브 시세·신호를 받아 예외 없이 서로
   다른 판단(기본형 손절 매도/공격적 신규 매수/모멘텀 관망)을 내는 것을 확인, `streamlit.
   testing.v1.AppTest`로 4개 탭 전환·지표 렌더링 예외 없음 확인, `streamlit run demo_app.py`
   실제 서버 기동 확인.

## 11. 모의투자 버전2(v2) — Claude Trading Skills 방법론 포팅

**상태: 설계만 완료, 구현 전 (2026-08-22).** 이 장은 독립된 새 기능이 아니라 v1과 **완전히
분리된 두 번째 실행·조회 파이프라인**이다 — 원장·워크플로·UI 모두 새로 만들고, v1의 기존
원장(`data/portfolio/`, `data/portfolio/aggressive/`, `data/portfolio/momentum/`)과 실행
스크립트(`scripts/run_daily_trading.py`)는 v1 관련 로직·값은 그대로 두고 v2 로직만 옆에
추가한다(11.5 참고 — 신호 계산은 공유해도 v1 판단·체결·원장 반영 경로는 손대지 않는다).

### 11.0 이 장이 존재하는 이유 — "스킬을 이용한다"의 재정의

사용자가 이전 대화에서 추천받았던 `tradermonty/claude-trading-skills`를 실제로 설치해보고
(본 세션 앞부분), "국내+미국+코인을 다 포함하는 플러그인"을 요청했을 때 조사 결과 **KRX를
지원하는 스킬은 존재하지 않는다**는 것이 확인됐다 — 모든 스킬이 미국 시장 API(FMP 등) 또는
크립토 API 기준이었다. 게다가 이 스킬들은 애초에 **Claude가 대화 세션에서 읽고 해석해
실행하는 마크다운 지침**이지, GitHub Actions cron이 매일 19:30 KST에 호출할 파이썬
함수가 아니다 — 문자 그대로 "스킬을 실행"하려면 매매 판단마다 Claude API를 호출해야 하고,
이는 PRD 9.1에 이미 "유료라서 지금은 보류"로 기록된 LLM 경로를 그대로 되살리는 것과
같다.

그래서 v2 착수 전에 세 가지 선택지(① 결정론적 포팅 — API 비용 $0, ② 실제 LLM 판단 — 월
$1~2대, ③ 설계 단계에서만 스킬 참고 + 실행은 결정론적)를 사용자에게 제시했고,
**①(결정론적 포팅)을 확정**했다. 즉 v2가 "tradermonty를 이용한다"는 것은:

- ✅ 스킬 문서에 적힌 **수식·임계값·판단 로직**을 읽고 그대로 파이썬 함수로 옮긴다
- ✅ 실행 시점엔 Claude API 호출이 전혀 없다 — v1과 동일하게 $0, 신규 시크릿 없음
- ❌ 스킬 스크립트(`scripts/*.py`)를 그대로 실행하지 않는다 — 대부분 FMP API 키가
  필요하고 미국 주식 전용이라 이 프로젝트의 KR+US+코인 데이터 계약과 맞지 않는다
- ❌ 스킬이 요구하는 입력(예: 주간 차트 이미지)을 그대로 쓰지 않는다 — 방법론(추세
  판정 기준, 사이징 공식)만 가져오고, 입력은 이 프로젝트가 이미 갖고 있는 일봉 OHLCV로
  대체한다

### 11.1 스킬 → 봇 매핑 (근거: 실제 설치된 SKILL.md 원문 확인)

v1과 동일하게 **기본형/공격적/모멘텀** 세 봇 구조를 유지한다. 판단 로직만 스킬 방법론
기반으로 새로 만들고, 시세·예측·뉴스감성 신호 계산과 리스크 가드레일은 v1과 최대한
공유한다(11.5).

| 봇 | 포팅 대상 스킬 | 가져오는 것 | 버리는 것(이 프로젝트에 안 맞음) |
|---|---|---|---|
| **기본형** | `technical-analyst` + `position-sizer` | MA 정렬(가격이 단기·중기 이평선 위, 이평선끼리도 정배열)로 추세를 분류하는 기준 / **ATR 기반 변동성 조정 손절폭**과 **계좌 리스크 1% 룰**로 포지션 크기를 정하는 공식(Fixed Fractional) | 주간 차트 이미지 입력, 확률 시나리오 리포트 생성(사람이 읽는 리포트이지 자동매매 입력이 아님) |
| **공격적** | `vcp-screener`(Minervini VCP) + `position-sizer` | **변동성 수축**(최근 N일 ATR이 그 이전 N일보다 좁아짐) + **N일 고점 근접**(피벗 부근)이라는 셋업 개념, ATR 배수를 넓히고 리스크%를 올리는 공격적 사이징. v1 공격적 봇의 **피라미딩 허용** 구조는 그대로 유지 | FMP API 의존 스크리닝 파이프라인, S&P500 유니버스 한정, 거래량 마른 정도의 정교한 스코어링(이 프로젝트 데이터로는 근사만 가능) |
| **모멘텀** | `stockbee-momentum-burst-screener` | **4% 브레이크아웃**(종가가 전일 대비 임계값 이상 급등) + **거래량 확장**(전일 대비) + **레인지 확장**(최근 3일 대비) + **종가 위치**(고가 근처 마감일수록 강한 신호)라는 4개 트리거 조합 | FMP 실시간 유니버스 스캔, 분 단위 조건, "시장 게이트"(별도 시장국면 판정 스킬 — 이 프로젝트엔 없음, 12장 향후 확장 후보) |

`backtest-expert`는 어느 봇의 런타임 로직에도 들어가지 않는다 — **11.7(0단계)** 검증
절차에서 방법론(파라미터 민감도, 최소 표본 수, 워크포워드)만 빌려 쓴다.

### 11.2 신규/변경 모듈

| 파일 | 상태 | 내용 |
|---|---|---|
| `src/trading_agent_v2.py` | 신규 | `decide_trades_v2_default` / `_aggressive` / `_momentum` + `TRADING_RULES_V2` 계열 + `BOT_STRATEGIES_V2` 레지스트리. **`trading_agent.py`는 뜯어고치지 않는다** — v1 코드는 8단계 완료 시점 그대로 둔다(라이브 원장을 매일 갱신 중인 코드라 리스크를 최소화). `infer_market()`·`TradeAction`·`apply_risk_guardrail()`은 `trading_agent.py`에서 그대로 import해 재사용(전략이 아니라 공통 유틸이므로 중복 정의하지 않는다). |
| `src/indicators.py` | 변경(함수 추가만) | `atr(df, window=14)` 신규 — 표준 Average True Range(전일 종가 기준 True Range의 이동평균). 기존 함수는 하나도 안 건드린다. market-agnostic이라 KR/US/코인 어디든 그대로 쓴다. |
| `config.py` | 변경(함수 추가만) | `portfolio_dir_for_v2(bot_id)` 신규 — **"default"를 포함한 세 봇 전부** `data/portfolio/v2/{bot_id}/`에 새 원장(1억원)으로 시작한다. 기존 `portfolio_dir_for()`(v1용, "default"만 특별 취급)는 그대로 둔다 — v2의 "기본형"이 v1의 라이브 원장(`data/portfolio/`)을 절대 재사용하지 않도록 함수 자체를 분리하는 것이 실수 방지책이다. |
| `scripts/run_daily_trading.py` | 변경(추가만) | `_build_signal()`에 v2 전용 필드(`atr14`, `atr_contraction_ratio`, `dist_from_high_pct`, `volume_ratio_1d`, `range_ratio_3d`, `close_location`) 추가 — 이미 갖고 있는 `price_df`에서 계산하므로 신규 네트워크 호출 없음(sma5_gap/sma20_gap과 동일한 패턴). `run()` 마지막에 v1 봇 루프와 나란히 v2 봇 루프를 추가(11.5). |
| `pages/모의투자_v2.py` | 신규 | v2 전용 리포팅 페이지. **"모의투자" 탭과 동일한 프레임**(요약 지표 4개 → 자산추이 차트 → 보유종목 → 거래내역 → 거래요약 → 성과비교 탭)을 쓰되, 데이터 소스는 `BOT_STRATEGIES_V2`/`portfolio_dir_for_v2`. |
| `src/portfolio_ui.py` | 신규(리팩터) | `pages/모의투자.py`의 `_render_bot_dashboard()`/`_render_comparison()`과 그 보조 함수들(`_change_html`, `_trades_show_df`, `_color_action`, `_stock_detail_dialog` 등)을 그대로 옮긴다 — 이미 `(bot_id, label, portfolio_dir)` 세 인자만 받는 순수 렌더 함수라 로직 변경 없이 이동만 하면 된다. `pages/모의투자.py`와 `pages/모의투자_v2.py` 둘 다 이 모듈을 import해서 **"기본 프레임 동일 유지"** 요구사항을 코드 중복 없이 만족시킨다. |
| `demo_app.py` | 변경(1줄 추가) | `st.Page("pages/모의투자_v2.py", title="모의투자(ver2)", icon="💰")`를 "모의투자" 바로 아래에 추가. |
| `.github/workflows/daily_trading.yml` | 변경 없음(확인만) | v2 봇 실행이 `run_daily_trading.py` 안에 통합되므로(11.5) 워크플로 자체는 안 바뀐다. `git add data/portfolio/`가 `data/portfolio/v2/**`까지 재귀로 잡는지만 `git add --dry-run`으로 재확인(8단계 때 한 번 걸렸던 함정 재발 방지). |

`.gitignore`는 **변경이 필요 없다** — 8단계에서 고친 `!data/portfolio/**/*.csv`는 이미
임의 깊이(`data/portfolio/v2/default/holdings.csv`)까지 재귀 매치하고, `state.json`은 애초에
`*.json` 전역 규칙이 없어(`git ls-files`로 확인) 별도 예외 없이도 추적된다. 구현
착수 시 `git add --dry-run data/portfolio/v2/`로 실제 추적 대상이 되는지 한 번 더
확인하는 것을 권장(위 표의 워크플로 행과 동일한 이유).

### 11.3 v2 리스크 규칙 — 핵심은 "고정 비중" → "리스크 기반 사이징"으로 전환

v1의 모든 봇은 포지션 크기를 **"총자산의 고정 %"**(`max_position_pct`)로 정했다. v2는
`position-sizer` 스킬의 핵심 아이디어인 **"몇 % 배분할지가 아니라 얼마나 잃을 각오를
하는지로 크기를 정한다"**(Fixed Fractional / ATR 기반)를 그대로 포팅한다:

```python
# src/trading_agent_v2.py (설계 — 아직 구현 전)
TRADING_RULES_V2_DEFAULT = {
    "risk_pct_per_trade": 0.01,     # 계좌 자산의 1% — position-sizer "1% rule"
    "atr_multiplier": 2.0,          # 손절 = 진입가 - ATR14 × 2.0 (position-sizer ATR 모드 기본값)
    "min_ma_alignment": True,       # 종가 > SMA20 > SMA60 (technical-analyst MA 정렬 기준을
                                     # 이 프로젝트가 이미 계산 중인 SMA20/60으로 포팅)
    "min_directional_accuracy": 0.55,   # v1과 동일 — 예측 신뢰도 필터는 그대로 유지
    "min_news_sentiment": -0.3,
    "take_profit_atr_multiplier": 3.0,  # 익절 = 진입가 + ATR14 × 3.0
    "max_rsi_entry": 70,
    "max_rsi_exit": 80,
    "exit_on_negative_signal": True,
    # 아래 4개는 v1 TRADING_RULES와 동일한 구조 — apply_risk_guardrail()을 그대로 재사용하기
    # 위해 반드시 같은 키 이름을 쓴다
    "max_position_pct": 0.15,
    "max_holdings": 10,
    "min_trade_amount": 1_000_000,
    "max_daily_trades": 5,
    "min_cash_reserve_pct": 0.05,
}

TRADING_RULES_V2_AGGRESSIVE = {
    **TRADING_RULES_V2_DEFAULT,
    "risk_pct_per_trade": 0.02,         # position-sizer "1% 기본, 예외적 사유 없인 2% 초과 금지" 상한을 그대로 상한값으로 채용
    "atr_multiplier": 2.5,
    "contraction_lookback": 10,          # ATR 수축 비교 구간(최근 10일 vs 이전 10일)
    "contraction_ratio_max": 0.85,       # 최근 ATR ≤ 이전 ATR × 0.85 → 수축으로 판정 (vcp-screener contraction_ratio 기본 0.70보다 완화 — 종목 수가 훨씬 적은 이 프로젝트 유니버스에서 과도하게 좁히면 후보가 0이 될 위험)
    "pivot_proximity_pct": 0.05,         # 최근 60일 고점 대비 -5% 이내
    "max_position_pct": 0.25,
    "max_holdings": 15,
    "max_daily_trades": 8,
    "min_cash_reserve_pct": 0.03,
}

TRADING_RULES_V2_MOMENTUM = {
    **TRADING_RULES_V2_DEFAULT,
    "risk_pct_per_trade": 0.01,          # 초안은 1.5%였으나 11.7 백테스트 실측 결과 MDD가
                                          # 과도해(-40.5%) 1.0%로 낮춤 — 근거는 11.7 참고
    "atr_multiplier": 1.5,               # 버스트 추종은 손절을 타이트하게 (stockbee 스타일 — 빠른 손절)
    "take_profit_atr_multiplier": 2.0,   # 짧게 먹고 나간다
    "burst_threshold": 0.04,             # stockbee "4% breakout" 원안 그대로 — 11.7에서 KR/US/코인
                                          # 유니버스로 백테스트해 과도하게 안 걸리는지 검증 필요
    "volume_ratio_min": 1.0,             # 거래량이 전일 이상
    "close_location_min": 0.6,           # (종가-저가)/(고가-저가) ≥ 0.6 — 고가 근처 마감
}
```

**포지션 수량 계산(v1 `_calc_quantity`를 대체하는 v2 전용 함수)**:

```python
risk_dollars = total_equity * rules["risk_pct_per_trade"]
stop_distance = atr14 * rules["atr_multiplier"]
quantity = risk_dollars / stop_distance    # 코인은 소수 8자리 반올림, 그 외는 정수 내림 — v1 _calc_quantity와 동일한 시장별 분기
target_amount = quantity * price
# 이후 max_position_pct 상한은 apply_risk_guardrail()이 그대로 재검증(변경 없음)
```

**의도적으로 스코프에서 뺀 것**:
- **Kelly Criterion 사이징**: `position-sizer`가 지원하지만 봇별 실측 승률/손익비가 최소
  30건 이상 쌓여야 의미 있다(`backtest-expert`의 표본 크기 원칙과 동일한 이유) — v2
  런칭 시점엔 데이터가 0건이므로 대상에서 제외하고, 향후 확장(11.12)으로 남긴다.
- **포트폴리오 히트 한도**(전체 미결제 리스크 ≤ 자산의 6~8%, `position-sizer` 원칙):
  `max_holdings × risk_pct_per_trade`가 이미 사실상 이 한도를 근사하므로(예: 기본형
  10종목 × 1% = 최대 10% 리스크) 별도 가드레일 필드를 새로 만들지 않는다 — 필요성이
  실측으로 확인되면 추가.
- 가중합 스코어링(0~100점) 대신 v1과 동일하게 **순차 임계값 필터**를 유지한다 — 경계값
  단위테스트가 쉽고(CLAUDE.md 테스트 규칙), 기존 `decide_trades*()` 코드 스타일과
  일관된다.

### 11.4 청산(매도) 조건 — ATR 기반으로 교체, 구조는 v1과 동일

세 봇 모두 v1과 같은 "순서대로 확인, 하나라도 맞으면 즉시 전량 매도" 구조를 유지하되,
손절·익절 기준이 고정 %가 아니라 ATR 배수다:

1. 손절: 현재가 ≤ 평단가 − `atr_multiplier` × 진입 시점 ATR14
2. 익절: 현재가 ≥ 평단가 + `take_profit_atr_multiplier` × 진입 시점 ATR14
3. 신호 소멸: 예측 5일 수익률 음전환 (v1과 동일)
4. RSI 과매수 청산 (v1과 동일)
5. **(모멘텀만) 추세 이탈**: 종가가 진입 후 최근 5일 저점 하회 — burst 전략은 짧은
   보유기간을 전제하므로 v1 모멘텀의 "SMA20 이탈"보다 더 촘촘한 윈도우를 쓴다

**진입 시점 ATR을 어디에 저장하나**: `holdings.csv` 스키마(`code, name, quantity,
avg_price`)에 컬럼을 추가하면 v1과 동일한 마이그레이션 리스크가 생긴다(8단계에서
`market` 컬럼을 일부러 안 넣은 것과 같은 이유). 대신 **매도 판단 시점에 그 종목의
"현재" ATR14를 다시 계산해서 쓴다**(진입 시점 값을 저장하지 않음) — 손절폭이 시간에 따라
조금씩 움직이지만, 코드 변경 없이 v1의 CSV 스키마를 그대로 재사용할 수 있는 실용적
절충이다. 이 근사가 실제로 문제가 되는지는 11.7 백테스트에서 확인한다.

### 11.5 실행 파이프라인 통합 (`run_daily_trading.py`)

**신호 계산은 공유하되, 판단·체결·원장 반영은 v1과 완전히 독립적으로 분리한다** — v1의
`run()`이 이미 "봇마다 원장이 분리돼 있고 한 봇의 예외가 다른 봇을 막지 않는다"는 원칙으로
짜여 있으므로, v2 봇들도 같은 원칙의 연장으로 추가한다:

```
run()
 ├─ (기존, 변경 없음) 국내/해외/코인 스냅샷 조회, 워치리스트 산출
 ├─ (기존, 변경 없음) _build_signal() 순회 → signals 생성
 │     └─ (신규) 여기서 atr14/atr_contraction_ratio/dist_from_high_pct/
 │              volume_ratio_1d/range_ratio_3d/close_location 필드도 함께 계산
 ├─ (기존, 변경 없음) for bot_id in pending v1 bots: _run_bot(...)   ← v1 3개 봇
 └─ (신규) for bot_id in pending v2 bots: _run_bot_v2(...)          ← v2 3개 봇
```

`signals`(예측·RSI·뉴스감성·ATR 등 전부)는 v1·v2가 **같은 리스트를 그대로 공유**한다 —
predictor 학습·뉴스 조회가 이 파이프라인에서 가장 비싼 부분인데, v2를 위해 이를 다시
돌리면 GitHub Actions 실행 시간과 네이버 뉴스 요청 수가 거의 두 배가 된다. `_run_bot_v2()`는
`_run_bot()`과 거의 동일한 구조(판단→가드레일→체결→시가평가→원장반영)이되
`trading_agent_v2.BOT_STRATEGIES_V2`와 `config.portfolio_dir_for_v2()`를 쓴다는 점만
다르다 — 코드 중복이 걱정되면 `_run_bot()`을 `strategies_registry`/`portfolio_dir_fn`
파라미터를 받도록 일반화하는 것도 고려할 수 있지만, **v1 라이브 코드 경로를 절대 건드리지
않는다는 원칙**을 우선해 이번엔 별도 함수로 복사해서 시작하고, 안정화된 뒤(11.11의 며칠
관찰 단계 이후) 리팩터링 여부를 재검토하는 것을 권장한다.

**실패 격리**: 국내(KRX) 스냅샷 실패 시 전체 중단은 v1과 동일하게 유지(기존 동작
변경 없음). v2 봇 하나의 예외가 v1 봇이나 다른 v2 봇을 막지 않도록 v1과 동일하게
봇 단위 `try/except`로 감싼다.

### 11.6 UI — "모의투자(ver2)" 탭

```python
# demo_app.py (변경 후)
pg = st.navigation([
    st.Page("pages/모니터링.py", title="모니터링", icon="🖥️"),
    st.Page("pages/모의투자.py", title="모의투자", icon="💰"),
    st.Page("pages/모의투자_v2.py", title="모의투자(ver2)", icon="💰"),  # 신규
])
```

`pages/모의투자_v2.py`는 `pages/모의투자.py`와 거의 동일한 골격이다(11.2의
`src/portfolio_ui.py` 공유로 실제 코드는 중복 없음):

- 상단 면책 문구는 v1과 동일 문구를 유지하되, "이 화면은 tradermonty Claude 스킬(포지션
  사이징·기술적 분석·모멘텀 스크리닝) 방법론을 결정론적으로 포팅한 두 번째 실험용
  매매 로직입니다. v1(모의투자 탭)과는 완전히 분리된 별도 계좌(1억원)이며 자금 이동이
  없습니다"라는 캡션을 추가로 노출 — 방문자가 두 탭을 서로 다른 실전략 비교로
  오인하지 않게 한다.
- `BOT_STRATEGIES_V2`를 순회해 봇 탭 3개 + "성과 비교" 탭 1개 (v1과 동일 구성)
- 원한다면 "v1 vs v2 기본형" 같은 교차 비교는 이번 스코프에서 제외(성과 비교 탭은 같은
  버전 안의 봇끼리만 비교) — 필요성이 확인되면 11.12 향후 확장으로

### 11.7 0단계 — 결정론적 백테스트 검증 (`backtest-expert` 방법론 적용)

v1의 "0단계는 코딩이 아니라 실측"이라는 원칙을 v2에도 그대로 적용한다. `backtest-expert`
스킬 문서가 강조하는 원칙 중 이 프로젝트에 적용 가능한 것만 추린다(FMP 기반 스크립트는
쓰지 않고, 이 프로젝트의 `data_loader`/`crypto_loader`/`us_screener` 과거 데이터로
자체 백테스트를 새로 짠다):

- **최소 표본**: 전략당 최소 30건의 신호(가급적 100건 이상) 없이는 결과를 신뢰하지 않는다
- **파라미터 민감도**: `burst_threshold`(3%/4%/5%), `atr_multiplier`(1.5x/2.0x/2.5x/3.0x)를
  베이스라인 대비 ±로 흔들어보고, 특정 값 하나에서만 잘 되는 "뾰족한 봉우리"가 아니라
  "완만한 고원"을 찾는다 — 뾰족하면 과최적화 신호
- **워크포워드**: 과거 구간을 훈련/검증으로 나눠, 검증 구간 성과가 훈련 구간의 절반
  미만으로 떨어지면 경고
- **비관적 가정**: 이 프로젝트는 이미 "그날 종가 체결"이라는 단순화를 쓰고 있으므로
  (5.3), 슬리피지를 추가로 얹어 보수적으로 본다

신규 스크립트 `scripts/backtest_v2_strategies.py`(로컬 전용, git 커밋 원장에 영향 없음)를
만들어 워치리스트 상위 종목들의 과거 1~2년 데이터로 세 봇의 신규 로직을 시뮬레이션하고,
`backtest-expert`의 5개 평가축(표본크기·기댓값·리스크관리·강건성·실행현실성)을 사람이
직접 채점해 Deploy/Refine/Abandon을 판단한다 — `backtest-expert`의 `evaluate_backtest.py`
스크립트 자체는 이 프로젝트 데이터 형식과 안 맞아 그대로 안 쓰고, 평가 축·체크리스트만
참고해 판단 기준으로 삼는다.

**실측 결과 (2026-08-23, `scripts/backtest_v2_strategies.py` 최초 실행) — 완료.** 국내
20+해외 15+코인 15 = 유니버스 50종목(유효 히스토리 확보 47종목) × 과거 2년 데이터로
검증했다.

- **실행 중 실제 버그 하나 발견·수정**: 종목 히스토리의 **마지막 날**에 진입 신호가
  뜨면 `_simulate_trade()`가 `last_idx == entry_idx`가 되어 `days_held=0`인 트레이드를
  반환했고, 호출부(`_run_bot_backtest`)의 커서가 전혀 전진하지 못해 같은 지점에서 동일한
  트레이드를 무한히 반복 생성했다 — 실측으로 프로세스 메모리 2GB대까지 폭주하며 응답
  없음 상태가 됐다(사용자가 이상 징후를 감지해 알려줘서 발견). `_simulate_trade()`가
  이 경우 `None`을 반환하도록 막고, 호출부도 `i += max(days_held, 1)`로 이중 방어선을
  둬서 고쳤다. 합성 데이터로 이 정확한 엣지케이스(마지막 날 진입 강제 유도)를 재현하는
  회귀 시나리오로 재발 안 함을 확인.
- **평가 방법론 자체의 오류도 하나 발견·수정**: 최초 실행에서 세 봇 모두 MDD가
  -97~99%로 나와 의심스러웠다 — 원인은 트레이드를 이어붙여 복리 계산할 때 매번 계좌
  전체(100%)를 거는 것처럼 계산했기 때문이었다. 실제로는 `position-sizer`식으로 거래당
  `risk_pct_per_trade`(1~2%)만 걸므로, 가격 등락률(`return_pct`)이 아니라 "손절에 정확히
  맞으면 -risk_pct_per_trade가 되도록" 정규화한 `equity_impact_pct`로 다시 계산하도록
  스크립트를 고쳤다(합성 데이터로 손절/익절 각각 예상한 정규화 값 근방이 나오는지
  검증). 이 교정 없이 나온 첫 실행 결과는 폐기 — 아래 수치는 교정 후 값이다.
- **판정 결과**:

  | 봇 | 표본 | 기대값(건당, 계좌기준) | MDD(근사) | 판정 |
  |---|---|---|---|---|
  | 기본형 | 780건 | +0.19% | -21.0% | ✅ Deploy |
  | 공격적 | 65건 | +0.98% | -15.7% | ✅ Deploy |
  | 모멘텀(risk 1.5%, 초안) | 872건 | +0.24% | **-40.5%** | 🔄 Refine (MDD 과도) |
  | 모멘텀(risk 1.0%로 조정) | 872건 | +0.16% | -29.1% | ✅ Deploy |

  모멘텀 봇은 `atr_multiplier`를 1.5~3.0 사이로 넓혀도 MDD가 -40.5%→-36.8% 수준까지만
  줄어들어 손절폭이 원인이 아니었다 — 진짜 원인은 진입 조건이 자주 걸려(트레이드
  872건, 세 봇 중 최다) 승률 50%대가 반복될 때 연속 손실 구간이 길어지는 것이었다.
  `risk_pct_per_trade`는 `equity_impact_pct` 계산식에 선형으로 들어가므로(같은 트레이드
  집합에 대해 배율만 바뀜) 0.5%~1.5% 사이로 다시 스윕해 MDD가 선형에 가깝게
  줄어드는 것을 확인했고, **1.0%가 -30% 문턱을 막 통과하는 지점**이라 이 값으로
  확정했다(11.3·11.8에 반영 완료). 모멘텀 봇의 "공격성"은 이제 리스크%가 아니라
  좁은 손절(`atr_multiplier=1.5`)·빠른 회전(짧은 보유기간)에서 나온다.
- **파라미터 민감도(backtest-expert "고원 vs 봉우리")**: 기본형 `atr_multiplier`
  1.5~3.0, 공격적 `atr_multiplier` 1.5~3.0, 모멘텀 `burst_threshold` 3~5% 모두 기대값이
  완만하게 변하는 고원 형태였다(특정 값 하나에서만 잘 되는 뾰족한 봉우리 없음) — 과최적화
  신호는 없었다.
- **스코프 한계 재확인**: 이 검증은 `predicted_return_5d`/`directional_accuracy`/
  `news_sentiment` 필터를 뺀 채로(11.7 서두에 명시한 스코프) 진행했으므로, 실제 운영
  시엔 이 필터들이 후보를 더 추려내 트레이드 빈도는 줄고(872건보다 적을 가능성) 품질은
  달라질 수 있다 — 위 수치를 "이대로 실전에서도 재현된다"가 아니라 "v2 고유 메커니즘
  자체가 무너지지 않았다"는 근거로 읽는다.

### 11.8 리스크 관리 정책 요약표 (v1 6장과 대응)

| 규칙 | 기본형 | 공격적 | 모멘텀 |
|---|---|---|---|
| 거래당 리스크(계좌 대비) | 1% | 2% | 1%(11.7 백테스트로 1.5%→1.0% 하향 조정) |
| 손절 ATR 배수 | 2.0x | 2.5x | 1.5x |
| 익절 ATR 배수 | 3.0x | (피라미딩이라 개별 익절 대신 종목당 비중 상한이 사실상의 상한 역할) | 2.0x |
| 종목당 최대 비중 | 15% | 25% | 15%(기본형과 동일) |
| 동시 보유 종목 수 | 10 | 15 | 10 |
| 1회 매매당 최소 금액 | 100만원 | 100만원 | 100만원 |
| 하루 최대 거래 횟수 | 5 | 8 | 5 |
| 현금 최소 보유 비율 | 5% | 3% | 5% |

하단 4개 행은 `apply_risk_guardrail()`을 그대로 재사용하기 위해 **v1과 완전히 같은 키·같은
값**을 쓴다(11.3 코드 블록 참고) — v1이 이미 검증한 가드레일 로직을 v2도 그대로
신뢰한다.

### 11.9 면책 조항

v1의 7장 면책 문구를 그대로 상속한다. 추가로:
- v2는 **아직 실전 검증 이력이 없는 신규 로직**이므로, v1보다 더 실험적이라는 점을
  UI(11.6)와 이 문서 양쪽에 명시한다.
- "tradermonty 스킬을 참고했다"는 것이 그 스킬의 신뢰성이나 백테스트 검증을 보증하지
  않는다 — 이 프로젝트가 독자적으로 11.7의 자체 백테스트를 거쳐야 배포한다.

### 11.10 테스트 계획

- `tests/test_trading_agent_v2.py`(신규): v1의 `test_trading_agent.py`와 동일한 스타일 —
  합성 신호로 ATR 기반 손절/익절 경계값, MA 정렬 필터, 변동성 수축 판정, 4% 브레이크아웃
  트리거를 정확한 경계(임계값 바로 위/아래)에서 단위테스트. 외부 API 없음(v1과 동일한
  이유로 모킹 불필요).
- `tests/test_indicators.py`: `atr()` 함수 회귀테스트(합성 OHLCV로 알려진 True Range 값과
  대조) 추가.
- `tests/test_portfolio.py`: `portfolio_dir_for_v2()` 원장 격리 테스트 추가(v1 원장과
  섞이지 않는지 — 8단계 때 추가한 다중 원장 격리 테스트 패턴 재사용).
- `--dry-run` 스모크: `run_daily_trading.py --dry-run`을 워치리스트 축소본으로 돌려 v1
  3봇 + v2 3봇 총 6봇이 모두 예외 없이 판단을 내리는지 확인, v1 쪽 출력이 이번 변경
  전후로 **완전히 동일한지**(회귀 없음) diff로 대조.
- `streamlit.testing.v1.AppTest`로 `pages/모의투자_v2.py` 렌더링(초기 상태 + 합성 원장
  상태) 확인, 이어서 `streamlit run demo_app.py` 실제 기동 후 "모의투자(ver2)" 탭 확인.

### 11.11 구현 단계 제안

0. ~~**0단계 (실측/검증)**: `src/indicators.py`에 `atr()` 추가 + 11.7의 백테스트 스크립트로
   세 봇 초안 규칙(11.3의 수치)을 과거 데이터에 검증, Deploy/Refine/Abandon 판정 후 필요시
   수치 조정.~~ **완료 (2026-08-23)**. `atr()` 회귀테스트 3개 통과(오프라인 전체 179개
   회귀 없음), `scripts/backtest_v2_strategies.py` 작성·실행 중 무한루프 버그와 MDD 계산
   방법론 오류를 각각 발견·수정(근거는 11.7 "실측 결과"). 최종 판정: 기본형·공격적은
   초안 그대로 ✅ Deploy, 모멘텀은 `risk_pct_per_trade`를 1.5%→1.0%로 낮춰 ✅ Deploy —
   11.3·11.8의 수치는 이미 이 결과를 반영해 갱신했다. 이제 1단계로 진행 가능.
1. ~~`src/trading_agent_v2.py` — `decide_trades_v2_*()` 3종 + `TRADING_RULES_V2_*` +
   `BOT_STRATEGIES_V2`. 경계값 단위테스트 포함.~~ **완료 (2026-08-23)**. `_calc_quantity_v2`
   ·기본형(MA정렬·ATR손절익절)·공격적(변동성수축·피라미딩·take_profit 없음)·모멘텀(4%
   버스트·거래량/레인지확장·종가위치·5일저점이탈) 경계값 32개 테스트 통과. v1
   `trading_agent.py`는 한 글자도 안 건드림(`TradeAction`/`infer_market`/
   `apply_risk_guardrail`만 import). `decide_trades_v2_*` → `apply_risk_guardrail()`
   연계도 수동 스모크로 확인(저변동성 종목은 종목당비중초과로 거부, 정상변동성은 승인).
2. ~~`config.portfolio_dir_for_v2()`, `run_daily_trading.py`의 `_build_signal()` 필드 확장 +
   `_run_bot_v2()`/v2 봇 루프 추가. `--dry-run`으로 v1 출력 회귀 없음 확인.~~ **완료
   (2026-08-23)**. `_build_signal()`이 신규 네트워크 호출 없이 같은 price_df에서 v2
   필드(atr14/sma20/sma60/atr_contraction_ratio/dist_from_60d_high/day_change/
   volume_ratio_1d/day_range/prev_range_max3/close_location/low5)까지 계산해 v1·v2가
   신호 하나를 공유하도록 확장. `.gitignore`의 `!data/portfolio/**/*.csv` 재귀 규칙이
   `data/portfolio/v2/`도 실제로 커버하는지 `git check-ignore -v`/`git add --dry-run`으로
   먼저 확인(8단계 때 걸렸던 함정 재발 방지 차원). 워치리스트를 8종목으로 줄인 실제
   `--dry-run` 통합 테스트로 **v1 3봇 + v2 3봇 총 6봇 전체가 예외 없이 판단**하는 것을
   확인 — v1 세 봇은 기존 로직 그대로(기본형 손절매도 4건, 공격적 매수 4건, 모멘텀은
   가드레일이 코인 과대비중 요청을 정상 거부) 동작해 v2 통합이 v1을 회귀시키지 않았음을
   실측 확인, v2 기본형은 실제로 MA정렬+ATR사이징 매수 판단을 냄(공격적·모멘텀은 이
   축소 유니버스에서 조건에 맞는 후보가 없어 관망 — 정상). `config.portfolio_dir_for_v2()`
   격리 테스트 2개 추가(`tests/test_portfolio.py`). 전체 오프라인 테스트 213개 통과.
3. ~~`src/portfolio_ui.py` 리팩터(11.2) — 먼저 `pages/모의투자.py`가 이 모듈을 쓰도록
   바꾸고 **v1 페이지가 리팩터 전후로 픽셀 단위까지 동일하게 렌더링되는지** AppTest로
   확인(순수 이동이므로 로직 변경이 없어야 함).~~ **완료 (2026-08-23)**. `_change_html`
   부터 `_render_comparison`까지 헬퍼·렌더 함수 488줄과 title/warning/caption 문구를
   `src/portfolio_ui.py`의 `render_page(bot_strategies, portfolio_dir_fn, *, title,
   extra_caption=None)`로 옮기면서, 원본과 새 위치를 `diff`로 대조해 **글자 하나까지
   동일함을 실측 확인**(리뷰가 아니라 diff 자체로 증명). `pages/모의투자.py`는 `render_page
   (BOT_STRATEGIES, config.portfolio_dir_for, title="💰 모의투자")` 한 줄 호출로 축소.
   `extra_caption` 파라미터는 v2 페이지가 4단계에서 실험적 버전 안내 문구를 추가할 때 쓴다.
   AppTest로 (a) 실제 라이브 원장(보유종목·거래내역 있음)과 (b) 완전히 빈 원장(첫 실행 전,
   "아직 없음" 분기) 두 상태 모두 예외 없이 렌더링되는 것을 확인, `streamlit run
   demo_app.py`를 8502 포트로 띄워 서버가 에러 없이 기동하는 것도 확인했다(Chrome 브라우저
   도구가 이번 세션에도 비활성화돼 있어 v1의 10장 4단계와 동일하게 AppTest 엘리먼트 트리
   검사로 스크린샷 확인을 대체). 전체 오프라인 테스트 213개 유지(회귀 없음), ruff 통과.
4. ~~`pages/모의투자_v2.py` + `demo_app.py` 1줄 추가. AppTest → `streamlit run demo_app.py`
   실제 확인.~~ **완료 (2026-08-23)**. `pages/모의투자_v2.py`는 `render_page(
   BOT_STRATEGIES_V2, config.portfolio_dir_for_v2, title="💰 모의투자 (ver2)",
   extra_caption=...)` 한 번 호출로 구성 — `extra_caption`은 "v2는 tradermonty 스킬 방법론을
   결정론적으로 포팅한 실험용 로직이며 v1과 자금이 분리돼 있고 아직 실전 검증 이력이
   없다"는 안내를 표준 면책 캡션 뒤에 덧붙인다. `demo_app.py`에 `st.Page("pages/
   모의투자_v2.py", title="모의투자(ver2)", icon="💰")` 한 줄 추가. AppTest로 (a) 빈 원장,
   (b) 합성 매수 1건+자산추이 2일치를 실제로 `portfolio.apply_trade`/`save_daily_result`로
   기록해 만든 populated 상태(테스트 후 삭제 — 실제 원장 아님) 둘 다 예외 없이 렌더링
   확인, `streamlit run demo_app.py`(8502 포트) 실제 기동 확인. 전체 오프라인 테스트
   213개 유지, ruff 통과.
5. 2단계에서 로컬 `--dry-run` 통합 스모크(6봇 전체)는 이미 확인 완료 — 남은 건 실제
   GitHub Actions 워크플로에서도(이미 있는 workflow 그대로) 정상 커밋되는지 하루 관찰.
6. 며칠 관찰 (v1의 10장 6단계와 동일한 이유) — v2 원장이 실제로 쌓이는지, 성과가 v1과
   구조적으로 다르게 나오는지(같은 신호에서 같은 판단만 반복한다면 포팅이 무의미했다는
   신호).

### 11.12 향후 확장 (v2 이후)

- **Kelly Criterion 사이징**: 각 v2 봇의 실거래가 30건 이상 쌓이면 `position-sizer`의
  Half-Kelly 공식으로 `risk_pct_per_trade`를 정적 값 대신 승률/손익비 기반 동적 값으로
  전환 검토.
- **포트폴리오 히트 한도**: 전체 미결제 리스크가 실측으로 6~8%를 자주 넘는 것이 확인되면
  `apply_risk_guardrail()`에 v2 전용 히트 체크를 추가.
- **VCP 변동성 수축 판정 고도화**: 지금은 ATR 비율로 근사하지만, `vcp-screener`가 쓰는
  T1/T2/T3 단계별 되돌림 폭 비교 같은 더 정교한 판정은 이 프로젝트 데이터로 재현
  가능한지 추가 조사 필요.
- **v1 vs v2 교차 비교 UI**: 같은 "기본형"이라는 이름의 v1/v2 성과를 나란히 비교하는
  화면(11.6에서 이번 스코프 제외로 결정한 것의 재검토).
